# Arduino

The Arduino controller is in:

```text
arm_controller/arm_controller.ino
```

It receives four joint angles over serial and handles the servo-specific
offsets, PWM ranges and channels internally.
